node_modules/
dist/
.output/
.wrangler/
.vite/

# Note: src/routeTree.gen.ts is generated by the TanStack Start plugin but is
# committed so `tsc`/CI works on a fresh checkout without a prior build. It is
# regenerated (identically) on every dev/build.

# Local secrets — never commit
.dev.vars
.env
.env.*
!.env.example

# Cloudflare types (generated by `wrangler types`)
worker-configuration.d.ts

# Logs / OS
*.log
.DS_Store
